# lookup main, query, sliding_query utils

# This utility transforms a timelog file in a readable format

#TODO: timelog file must be confirmed before decoding time into readable format
#Script to read file and convert to readible format
